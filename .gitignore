node_modules/
.next/
out/
build/
.DS_Store
*.log

# env
.env
.env.local
.env.*.local

# editor
.vscode/
.idea/

# typescript
*.tsbuildinfo
next-env.d.ts

# 페어링 스냅샷 (런타임 데이터, 환경별로 다름)
data/snapshots/

# 디자인 원본 (PSD/AI/PDF — 대용량, 런타임 미사용)
DashDesignTemplates/02/

# bkit 플러그인 로컬 상태
.bkit/

# Claude Code 로컬 설정
.claude/

# Playwright 스크린샷(scripts/capture-steps.mjs) 출력 — 로컬에서만 사용
screenshots/
screenshots.zip

# 디자인 참고용 정적 HTML — JWT 등 민감 토큰 포함 가능, 리포에 올리지 않음
UI기준.html

# 레거시 백업 데이터 — 운영 데이터, 리포에 올리지 않음
data/judges-legacy-backup.json

# 심사위원 소개 영상 등 로컬 영상파일 — 대용량, /api/video 로 서빙
Video/
