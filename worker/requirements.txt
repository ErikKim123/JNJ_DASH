# AI Judge 분석 워커 — Python 3.11 권장
#
# 설치:
#   python -m venv worker/.venv
#   worker/.venv/Scripts/activate      (Windows)
#   pip install -r worker/requirements.txt
#
# 시스템 의존성: ffmpeg (PATH 에 있어야 함) — 영상에서 오디오 트랙 추출용

# ── DB / 스토리지 ────────────────────────────────────────────────
# 큐·리포트는 psycopg 로 Postgres 에 직접 붙는다.
#   PostgREST(exposed schemas) 설정에 의존하지 않고, FOR UPDATE SKIP LOCKED 를
#   그대로 쓸 수 있어 큐 구현이 단순해진다. (design §2.3 데비에이션 D-1)
psycopg[binary]>=3.2,<4
# Storage(영상 다운로드 / 키프레임 업로드) 는 Supabase SDK 사용
supabase>=2.15,<3

# ── 분석 ────────────────────────────────────────────────────────
mediapipe>=0.10.21
librosa>=0.10.2
soundfile>=0.12
opencv-python>=4.10
# mediapipe 가 numpy 2.x 에서 import 오류를 내면 numpy<2.0 으로 내려 고정할 것
numpy>=1.26

# ── 코멘트 / 알림 ────────────────────────────────────────────────
anthropic>=0.69
httpx>=0.27

# ── 유틸 / 테스트 ────────────────────────────────────────────────
jsonschema>=4.23
pytest>=8.3
